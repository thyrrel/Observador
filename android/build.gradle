// Conteúdo para android/build.gradle

// ADICIONE ESTE BLOCO INTEIRO
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    // NÃO DECLARE PLUGINS AQUI
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
