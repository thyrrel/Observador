// Conteúdo para android/build.gradle

plugins {
    // Este bloco deve vir antes de 'allprojects'
    // Ele pode ficar vazio, mas precisa estar aqui.
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
