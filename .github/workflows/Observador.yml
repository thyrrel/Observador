name: Observador

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build_android:
    name: Build Android
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Configurar Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'

      - name: Instalar dependências
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Build App Bundle (.aab)
        run: flutter build appbundle --release

      - name: Upload artefatos (APK e AAB)
        uses: actions/upload-artifact@v4
        with:
          name: android-build
          path: build/app/outputs/**/*.apk
          retention-days: 7

      # -----------------------------
      # 🚀 Publicação na Google Play (Comentada)
      # -----------------------------
      # - name: Deploy to Google Play
      #   uses: r0adkll/upload-google-play@v1
      #   with:
      #     serviceAccountJson: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT }}
      #     packageName: com.seuprojeto.observador
      #     releaseFiles: build/app/outputs/bundle/release/app-release.aab
      #     track: internal
      #     status: completed

  build_ios:
    name: Build iOS
    runs-on: macos-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'

      - name: Instalar dependências
        run: flutter pub get

      - name: Build iOS (IPA)
        run: flutter build ipa --release --no-codesign

      - name: Upload artefato (IPA)
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: build/ios/ipa/*.ipa
          retention-days: 7

      # -----------------------------
      # 🚀 Publicação no TestFlight (Comentada)
      # -----------------------------
      # - name: Instalar Fastlane
      #   run: sudo gem install fastlane -NV
      #
      # - name: Upload to TestFlight
      #   run: |
      #     fastlane pilot upload \
      #       --api_key_path ./fastlane/api_key.json \
      #       --ipa build/ios/ipa/Observador.ipa \
      #       --skip_submission true \
      #       --skip_waiting_for_build_processing true
