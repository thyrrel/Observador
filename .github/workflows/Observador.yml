name: Observador

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest

    env:
      PUB_CACHE: ${{ github.workspace }}/.pub-cache

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.13.5' # versão recomendada para Dart 3.5+

    - name: Enable Flutter
