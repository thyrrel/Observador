name: observador
description: Monitoramento avançado de redes e dispositivos
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.1.1
  flutter_local_notifications: ^12.0.4
  flutter_secure_storage: ^9.0.0
  sqflite: ^2.2.8+4
  path_provider: ^2.0.14
  pdf: ^7.6.0
  workmanager: ^0.7.0
  flutter_blue: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/docs/
