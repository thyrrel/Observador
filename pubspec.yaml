name: observador
description: Aplicativo de monitoramento e análise de rede
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  provider: ^6.0.6
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^13.0.0
  local_auth: ^2.2.1
  flutter_secure_storage: ^8.0.0
  sqflite: ^2.2.8+4
  path_provider: ^2.0.14
  pdf: ^3.11.3
  flutter_blue: ^0.8.0
  workmanager: ^0.6.4
  http: ^1.1.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
