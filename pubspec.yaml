name: observador
description: App de Smart Home Observador
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue: ^0.9.4
  connectivity_plus: ^4.0.0
  flutter_secure_storage: ^9.0.0
  http: ^1.1.0
  share_plus: ^6.5.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
