name: Observador
description: "Um aplicativo de arquitetura modular para monitoramento de redes."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  dependencies:
  flutter:
    sdk: flutter
  # --- NOVAS ---
  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  http: ^1.1.0
  in_app_purchase: ^3.1.6
  flutter_local_notifications: ^16.2.0
  syncfusion_flutter_charts: ^24.2.5
  cupertino_icons: ^1.0.6



