name: observador
description: App de monitoramento smart de rede
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.6
  http: ^1.1.0
  flutter_local_notifications: ^13.0.0
  sqflite: ^2.2.8+3
  path: ^1.9.3
  pdf: ^3.12.1
  flutter_secure_storage: ^9.0.1
  workmanager: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
