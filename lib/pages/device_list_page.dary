// /lib/pages/device_list_page.dart
// ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
// ┃ 📋 DeviceListPage - Tela principal           ┃
// ┃ 🔧 Lista de dispositivos monitorados         ┃
// ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../providers/network_provider.dart';
import '../widgets/device_tile.dart';
import '../widgets/loading_indicator.dart';
import 'device_details_page.dart';

class DeviceListPage extends StatelessWidget {
  const DeviceListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dispositivos na Rede')),

      body: Consumer<NetworkProvider>(
        builder: (_, provider, __) {
          final devices = provider.devices;

          if (devices.isEmpty) {
            return const LoadingIndicator();
          }

          return ListView.builder(
            itemCount: devices.length,
            itemBuilder: (_, index) {
              final device = devices[index].device;

              return GestureDetector(
                onTap: () {
                  Navigator.push(
                    context,
                    MaterialPageRoute(
                      builder: (_) => DeviceDetailsPage(device: device),
                    ),
                  );
                },
                child: DeviceTile(device: device),
              );
            },
          );
        },
      ),
    );
  }
}
